"""Flet (Version2) UI for GameTracker.

The new UI lives entirely under this package and talks to the backend only
through ``core.services``. The legacy PySimpleGUI UI (main.py, ui_components.py,
event_handlers.py, *_ui.py, game_hub.py, ...) is left in place during migration
and removed once this UI reaches feature parity.
"""
