# Games List Manager Dependencies

# Core GUI framework (version 4.x only - version 5.x has breaking changes)
PySimpleGUI>=4.60.0,<5.0.0

# Data visualization
matplotlib>=3.5.0

# Excel file support
openpyxl>=3.0.0

# Image processing for emoji rendering
Pillow>=9.0.0

# Discord Rich Presence integration
pypresence>=4.2.0

# HTTP requests for downloading images
requests>=2.25.0

# Process watcher: enumerate running processes (cross-platform)
psutil>=5.9.0

# Process watcher: fast fuzzy string matching for resolver layer 3
rapidfuzz>=3.0.0

# Process watcher: actionable Windows toast notifications.
# Soft-imported at runtime; non-Windows installs can omit it.
windows-toasts>=1.1.0; sys_platform == "win32"

# Process watcher: system-tray icon (cross-platform)
pystray>=0.19.0

# Standard library modules (included with Python)
# - datetime
# - json
# - os
# - sys
# - platform
# - base64
# - io
# - tempfile
# - collections
# - re
# - sqlite3 (used by store_manifests for GOG Galaxy DB)
# - winreg  (Windows-only, used by store_manifests for GOG registry fallback)
